# Backend base URL for frontend API/socket/docs calls
# Example (deployment):
# VITE_BACKEND_URL=https://your-backend-domain.com

# Local fallback is handled in code when this variable is missing.
VITE_BACKEND_URL=
